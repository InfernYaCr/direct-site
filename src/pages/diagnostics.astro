---
import Base from '@/layouts/Base.astro';
import Footer from '@/components/sections/Footer.astro';
import CookieBanner from '@/components/ui/CookieBanner.astro';

const painCards = [
  {
    tag: 'Деньги',
    title: 'Бюджет уходит, кресла пустуют',
    body: 'Вы уже платили агентствам по 60–150 тысяч в месяц, видели отчёты с кликами и охватами, но не видели нормального потока пациентов.',
  },
  {
    tag: 'Заявки',
    title: 'Звонят не те люди',
    body: 'Администратор слышит вопросы про бесплатное удаление, дешёвые акции и случайные консультации вместо заявок на имплантацию.',
  },
  {
    tag: 'Контроль',
    title: 'Приходится проверять всё самому',
    body: 'Вечером вы снова открываете отчёты и Excel, потому что уже не понимаете, кому можно доверять рекламный кабинет.',
  },
];

const leakCards = [
  'Алгоритм Яндекса или ВК первые 3–4 недели обучается на вашем бюджете.',
  'Реклама показывается школьникам, пенсионерам и людям с дешёвыми запросами.',
  'Вы оплачиваете клики людей, которые никогда не купят имплантацию за 80 000 ₽.',
  'Через месяц агентство просит ещё времени на тесты, а лизинг и аренда продолжают капать.',
];

const navigatorBlocks = [
  {
    title: 'Данные-доноры',
    body: 'Готовые аудитории людей, которые уже платили за дорогое лечение.',
  },
  {
    title: 'Офферы под стоматологию',
    body: 'Связки под имплантацию, виниры, All-on-4 и ортодонтию.',
  },
  {
    title: 'Модерация',
    body: 'Креативы, которые легче проходят правила медицинской рекламы.',
  },
  {
    title: 'Минус-фразы',
    body: '200+ исключений против запросов «бесплатно», «дёшево», «по акции».',
  },
];

const comparisonRows = [
  ['3–4 недели обучения за ваш счёт', 'Заявки уже на 3–5 день'],
  ['Трафик «всем подряд»', 'Платёжеспособные пациенты'],
  ['Запросы про бесплатное удаление', 'Имплантация, виниры, All-on-4'],
  ['CAC 8 000–15 000 ₽', 'CAC до 4 000 ₽ за запись'],
  ['«Нужно ещё потестить»', 'Прогнозируемые 30–50 пациентов в месяц'],
];

const auditItems = [
  'Покажу, где конкретно сливаются деньги в кабинете.',
  'Посчитаем, сколько сейчас стоит пациент и сколько он должен стоить.',
  'Оценим, можно ли выйти на 30–50 платёжеспособных записей в месяц.',
  'Соберём план внедрения «Умного Навигатора» под вашу клинику.',
];

const callSteps = [
  ['5 мин', 'Смотрим кабинет', 'Я.Директ или ВК: кампании, бюджеты, ставки, аудитории.'],
  ['10 мин', 'Находим слив', 'Запросы, аудитории, минус-фразы и участки, где деньги уходят впустую.'],
  ['8 мин', 'Считаем экономику', 'Текущая цена пациента, нормальная рыночная цена и потери за месяц.'],
  ['7 мин', 'Собираем план', 'Какие связки подойдут под имплантацию, виниры, ортодонтию или All-on-4.'],
];

const photoNotes = [
  'Hero: Ростислав в клинике или светлый стоматологический кабинет.',
  'Разбор: скрин рекламного кабинета с замазанными данными и пометками.',
  'Автор: живой портрет Ростислава без стоковой постановки.',
  'Финал: фото созвона или ноутбука с открытым аудитом.',
];
---

<Base
  title="Диагностика рекламы для стоматологий"
  description="30-минутная диагностика рекламного кабинета ВК и Яндекс.Директ для владельцев стоматологических клиник."
>
  <main id="main-content" class="med-page">
    <section class="med-hero" aria-labelledby="med-hero-title">
      <div class="container med-hero__inner">
        <div class="med-hero__content">
          <p class="med-pill">30 минут · ВК / Яндекс.Директ · для стоматологий</p>
          <h1 id="med-hero-title">Сливаете 100 000 ₽/мес на рекламу, а в кресле пусто?</h1>
          <p class="med-hero__lead">
            Покажу, где утекают деньги, и как получать 30–50 платёжеспособных пациентов в месяц на имплантацию по цене до 1 500 ₽ за запись.
          </p>
          <div class="med-hero__actions">
            <a class="med-btn" href="#diagnostics-form">Записаться на диагностику</a>
            <span>Обычная цена 4 990 ₽. Сейчас 0 ₽ для владельцев стоматологий.</span>
          </div>
        </div>

        <aside class="med-hero__panel" aria-label="Ключевые показатели диагностики">
          <div class="med-photo med-photo--hero">
            <span>Место под фото: эксперт в клинике</span>
          </div>
          <div class="med-metrics">
            <article>
              <strong>3–5 день</strong>
              <span>первые заявки после запуска связок</span>
            </article>
            <article>
              <strong>200+</strong>
              <span>минус-фраз против дешёвых запросов</span>
            </article>
          </div>
        </aside>
      </div>
    </section>

    <section class="med-section med-pain" aria-labelledby="med-pain-title">
      <div class="container">
        <div class="med-section__head">
          <p class="med-pill">Ситуация владельца</p>
          <h2 id="med-pain-title">Проблема выглядит не как “нет рекламы”. Проблема выглядит как отсутствие доверия к рекламе.</h2>
        </div>

        <div class="med-pain__grid">
          {painCards.map((card) => (
            <article class="med-card">
              <span>{card.tag}</span>
              <h3>{card.title}</h3>
              <p>{card.body}</p>
            </article>
          ))}
        </div>

        <p class="med-note">
          Конкурент через дорогу демпингует «имплант за 19 900», сарафан закончился, а новый маркетолог снова обещает лиды по 300 рублей.
        </p>
      </div>
    </section>

    <section class="med-section med-leak" aria-labelledby="med-leak-title">
      <div class="container med-leak__inner">
        <div class="med-leak__copy">
          <p class="med-pill">Настоящая причина</p>
          <h2 id="med-leak-title">Алгоритм учится вслепую. Вы оплачиваете каждый его тычок.</h2>
          <p>
            Дело не в том, что реклама в стоматологии больше не работает. Обычно кабинет запускают с нуля, и первые недели площадка сама пытается понять, кого искать.
          </p>
        </div>

        <div class="med-audit-card">
          <div class="med-photo med-photo--audit">
            <span>Место под скрин: кабинет с пометками аудита</span>
          </div>
          <h3>Что видно на разборе</h3>
          <ul>
            {leakCards.map((item) => <li>{item}</li>)}
          </ul>
        </div>
      </div>
    </section>

    <section class="med-section med-method" aria-labelledby="med-method-title">
      <div class="container">
        <div class="med-method__intro">
          <p class="med-pill">Решение</p>
          <h2 id="med-method-title">«Умный Навигатор» даёт алгоритму ориентиры с первого дня</h2>
          <p>
            Я не начинаю с пустого кабинета. В запуск идут готовые данные, офферы, аудитории, креативы и минус-фразы, накопленные за 6 лет маркетинга и 10+ проектов в медицине.
          </p>
        </div>

        <div class="med-method__diagram" aria-label="Схема метода Умный Навигатор">
          {navigatorBlocks.map((block, index) => (
            <article>
              <span>{index + 1}</span>
              <h3>{block.title}</h3>
              <p>{block.body}</p>
            </article>
          ))}
        </div>
      </div>
    </section>

    <section class="med-section med-compare" aria-labelledby="med-compare-title">
      <div class="container">
        <div class="med-section__head">
          <p class="med-pill">До / после</p>
          <h2 id="med-compare-title">Что меняется в запуске</h2>
        </div>

        <div class="med-compare__list">
          {comparisonRows.map(([before, after]) => (
            <article>
              <p><span>Обычно</span>{before}</p>
              <p><span>С навигатором</span>{after}</p>
            </article>
          ))}
        </div>
      </div>
    </section>

    <section class="med-section med-offer" aria-labelledby="med-offer-title">
      <div class="container med-offer__inner">
        <div>
          <p class="med-pill">Бесплатная диагностика</p>
          <h2 id="med-offer-title">За 30 минут вы получите не “консультацию”, а разбор кабинета с цифрами</h2>
        </div>

        <div class="med-offer__items">
          {auditItems.map((item) => (
            <article>
              <svg viewBox="0 0 24 24" aria-hidden="true"><path d="m5 12 4 4L19 6" /></svg>
              <p>{item}</p>
            </article>
          ))}
        </div>
      </div>
    </section>

    <section class="med-section med-steps" aria-labelledby="med-steps-title">
      <div class="container med-steps__inner">
        <div class="med-section__head">
          <p class="med-pill">Сценарий созвона</p>
          <h2 id="med-steps-title">Как пройдёт диагностика</h2>
        </div>

        <div class="med-steps__timeline">
          {callSteps.map(([time, title, body]) => (
            <article>
              <span>{time}</span>
              <h3>{title}</h3>
              <p>{body}</p>
            </article>
          ))}
        </div>
      </div>
    </section>

    <section class="med-section med-author" aria-labelledby="med-author-title">
      <div class="container med-author__inner">
        <figure class="med-author__media">
          <div class="med-photo med-photo--portrait">
            <span>Место под фото: Ростислав Загребин</span>
          </div>
        </figure>

        <div class="med-author__copy">
          <p class="med-pill">Кто проводит разбор</p>
          <h2 id="med-author-title">Ростислав Загребин</h2>
          <p>
            6 лет в маркетинге, фокус — Яндекс.Директ и ВК для медицины. На первом клиенте сделал х10 по чистой прибыли за 1,5 года через Я.Директ и сайт.
          </p>
          <div class="med-author__facts">
            <span>10+ проектов в медицине</span>
            <span>Бюджеты от 300 000 ₽/мес</span>
            <span>29+ ниш для переноса связок</span>
            <span>Сертификация по Яндекс.Директ</span>
          </div>
        </div>
      </div>
    </section>

    <section id="diagnostics-form" class="med-section med-final" aria-labelledby="med-final-title">
      <div class="container med-final__inner">
        <div class="med-final__copy">
          <p class="med-pill">Финальный шаг</p>
          <h2 id="med-final-title">Каждый день без нормально настроенной рекламы стоит вам денег</h2>
          <p>
            Худшее, что может случиться: вы узнаете, где сейчас теряете деньги, и почините это сами. Никаких менеджеров и отделов продаж — пишу или звоню лично я.
          </p>

          <div class="med-bonus">
            <strong>Бонус первым 3 клиникам</strong>
            <span>Стратегическая консультация «Как удвоить поток пациентов за 14 дней без увеличения бюджета».</span>
          </div>

          <div class="med-photo med-photo--call">
            <span>Место под фото: созвон или рабочий стол с аудитом</span>
          </div>
        </div>

        <form class="med-form" aria-label="Заявка на бесплатную диагностику">
          <div>
            <span>Свяжусь в течение 10 минут</span>
            <h3>Заявка на диагностику</h3>
          </div>
          <label>
            Ваше имя
            <input name="name" type="text" autocomplete="name" />
          </label>
          <label>
            Телефон или Telegram
            <input name="contact" type="text" autocomplete="tel" />
          </label>
          <label>
            Город и сайт клиники
            <input name="clinic" type="text" />
          </label>
          <label>
            Что сейчас происходит с рекламой?
            <textarea name="message" rows="4"></textarea>
          </label>
          <button type="button">Записаться на бесплатную диагностику</button>
          <p>Если реклама ещё не настроена, тоже приходите: покажу, с чего начать без слива бюджета.</p>
        </form>
      </div>
    </section>

    <section class="med-photo-guide" aria-labelledby="med-photo-guide-title">
      <div class="container med-photo-guide__inner">
        <h2 id="med-photo-guide-title">Места под реальные материалы</h2>
        <ul>
          {photoNotes.map((note) => <li>{note}</li>)}
        </ul>
      </div>
    </section>

    <a class="med-sticky" href="#diagnostics-form">Записаться</a>
  </main>

  <Footer />
  <CookieBanner />
</Base>

<style>
  :global(body) {
    background: #f7f8f3;
    color: #16211f;
  }

  .med-page {
    --ink: #16211f;
    --muted: #63706c;
    --line: rgba(22, 33, 31, 0.12);
    --paper: #fffefa;
    --soft: #edf7f1;
    --blue: #eaf3fb;
    --accent: #e94b1b;
    --green: #12724b;
    --shadow: 0 18px 50px rgba(27, 55, 47, 0.09);
    background: linear-gradient(180deg, #f7f8f3 0%, #fffefa 42%, #f4faf6 100%);
  }

  .med-page :global(section:nth-of-type(even)) {
    background: transparent;
  }

  .med-section {
    padding-block: 88px;
  }

  .med-pill {
    display: inline-flex;
    margin: 0 0 18px;
    padding: 8px 12px;
    border: 1px solid var(--line);
    border-radius: 999px;
    background: rgba(255, 254, 250, 0.84);
    color: var(--green);
    font-size: 13px;
    font-weight: 800;
    letter-spacing: 0;
    line-height: 1.2;
  }

  .med-page h1,
  .med-page h2,
  .med-page h3,
  .med-page p {
    letter-spacing: 0;
  }

  .med-page h1,
  .med-page h2 {
    margin: 0;
    color: var(--ink);
    font-weight: 900;
    line-height: 1.02;
  }

  .med-page h1 {
    max-width: 740px;
    font-size: 76px;
  }

  .med-page h2 {
    max-width: 880px;
    font-size: 54px;
  }

  .med-page h3 {
    margin: 0;
    color: var(--ink);
    font-size: 22px;
    line-height: 1.1;
  }

  .med-page p {
    color: var(--muted);
    line-height: 1.55;
  }

  .med-section__head {
    max-width: 880px;
    margin-block-end: 34px;
  }

  .med-btn,
  .med-form button,
  .med-sticky {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 54px;
    padding: 14px 20px;
    border: 0;
    border-radius: 8px;
    background: var(--accent);
    color: #fff;
    font: inherit;
    font-weight: 900;
    letter-spacing: 0;
    text-decoration: none;
    cursor: pointer;
    box-shadow: 0 14px 30px rgba(233, 75, 27, 0.24);
  }

  .med-hero {
    padding-block: 56px 48px;
    background:
      radial-gradient(circle at 80% 8%, rgba(18, 114, 75, 0.16), transparent 30%),
      linear-gradient(135deg, #f7f8f3, #eff8f2 58%, #eaf3fb);
  }

  .med-hero__inner {
    display: grid;
    grid-template-columns: minmax(0, 1.05fr) minmax(360px, 0.72fr);
    gap: 44px;
    align-items: end;
    min-height: 760px;
  }

  .med-hero__lead {
    max-width: 720px;
    margin: 24px 0 0;
    color: #344642;
    font-size: 24px;
  }

  .med-hero__actions {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 18px;
    margin-block-start: 32px;
  }

  .med-hero__actions span {
    max-width: 330px;
    color: var(--muted);
    line-height: 1.45;
  }

  .med-hero__panel {
    display: grid;
    gap: 14px;
  }

  .med-photo {
    display: grid;
    min-height: 280px;
    border: 1px solid rgba(22, 33, 31, 0.16);
    border-radius: 8px;
    overflow: hidden;
    place-items: center;
    background:
      linear-gradient(135deg, rgba(255, 255, 255, 0.86), rgba(234, 243, 251, 0.72)),
      repeating-linear-gradient(135deg, rgba(22, 33, 31, 0.08) 0 1px, transparent 1px 16px);
  }

  .med-photo span {
    max-width: 260px;
    padding: 12px 14px;
    border-radius: 8px;
    background: rgba(255, 254, 250, 0.88);
    color: #40504b;
    font-weight: 800;
    line-height: 1.35;
    text-align: center;
  }

  .med-photo--hero {
    min-height: 520px;
    background:
      linear-gradient(160deg, rgba(237, 247, 241, 0.84), rgba(234, 243, 251, 0.86)),
      linear-gradient(90deg, transparent 0 58%, rgba(255, 255, 255, 0.72) 58% 61%, transparent 61%),
      repeating-linear-gradient(0deg, rgba(22, 33, 31, 0.08) 0 1px, transparent 1px 44px);
  }

  .med-metrics {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 14px;
  }

  .med-metrics article,
  .med-card,
  .med-audit-card,
  .med-method__diagram article,
  .med-compare__list article,
  .med-offer__items article,
  .med-steps__timeline article,
  .med-form,
  .med-bonus {
    border: 1px solid var(--line);
    border-radius: 8px;
    background: rgba(255, 254, 250, 0.9);
    box-shadow: var(--shadow);
  }

  .med-metrics article {
    padding: 18px;
  }

  .med-metrics strong {
    display: block;
    color: var(--ink);
    font-size: 26px;
    line-height: 1;
  }

  .med-metrics span {
    display: block;
    margin-block-start: 8px;
    color: var(--muted);
    line-height: 1.35;
  }

  .med-pain__grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 18px;
  }

  .med-card {
    padding: 24px;
  }

  .med-card span {
    display: block;
    margin-block-end: 34px;
    color: var(--accent);
    font-weight: 900;
  }

  .med-card p {
    margin: 16px 0 0;
  }

  .med-note {
    margin: 22px 0 0;
    padding: 22px;
    border-left: 4px solid var(--accent);
    border-radius: 8px;
    background: #fff4ea;
    color: #5a3728;
    font-size: 22px;
    font-weight: 800;
  }

  .med-leak {
    background: linear-gradient(180deg, #fffefa, #f4faf6);
  }

  .med-leak__inner,
  .med-offer__inner,
  .med-author__inner,
  .med-final__inner {
    display: grid;
    grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.1fr);
    gap: 48px;
    align-items: start;
  }

  .med-leak__copy p,
  .med-method__intro p,
  .med-final__copy > p,
  .med-author__copy > p {
    max-width: 660px;
    font-size: 21px;
  }

  .med-audit-card {
    padding: 18px;
  }

  .med-photo--audit {
    min-height: 320px;
    margin-block-end: 20px;
  }

  .med-audit-card ul {
    display: grid;
    gap: 12px;
    margin: 16px 0 0;
    padding: 0;
    list-style: none;
  }

  .med-audit-card li {
    padding: 14px;
    border-radius: 8px;
    background: #f7f8f3;
    color: #31433e;
    line-height: 1.45;
  }

  .med-method__intro {
    max-width: 900px;
    margin-block-end: 34px;
  }

  .med-method__diagram {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 14px;
  }

  .med-method__diagram article {
    min-height: 250px;
    padding: 22px;
  }

  .med-method__diagram article:nth-child(1) { background: #edf7f1; }
  .med-method__diagram article:nth-child(2) { background: #eaf3fb; }
  .med-method__diagram article:nth-child(3) { background: #fffefa; }
  .med-method__diagram article:nth-child(4) { background: #fff2cf; }

  .med-method__diagram span {
    display: grid;
    width: 42px;
    height: 42px;
    margin-block-end: 48px;
    border-radius: 50%;
    place-items: center;
    background: var(--ink);
    color: #fff;
    font-weight: 900;
  }

  .med-method__diagram p {
    margin: 14px 0 0;
  }

  .med-compare {
    background: #edf7f1;
  }

  .med-compare__list {
    display: grid;
    gap: 12px;
  }

  .med-compare__list article {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    overflow: hidden;
  }

  .med-compare__list p {
    margin: 0;
    padding: 18px;
    color: var(--ink);
    font-size: 20px;
    font-weight: 800;
  }

  .med-compare__list p:first-child {
    background: #fffefa;
  }

  .med-compare__list p:last-child {
    background: #e2f6ea;
    color: #0f5d3d;
  }

  .med-compare__list span {
    display: block;
    margin-block-end: 6px;
    color: var(--muted);
    font-size: 13px;
    font-weight: 900;
  }

  .med-offer__items {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 14px;
  }

  .med-offer__items article {
    padding: 20px;
  }

  .med-offer__items svg {
    width: 28px;
    height: 28px;
    margin-block-end: 22px;
    color: var(--green);
  }

  .med-offer__items path {
    fill: none;
    stroke: currentColor;
    stroke-width: 2.4;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .med-offer__items p {
    margin: 0;
    color: var(--ink);
    font-weight: 800;
  }

  .med-steps {
    background: #f4f8fb;
  }

  .med-steps__timeline {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 14px;
  }

  .med-steps__timeline article {
    padding: 22px;
  }

  .med-steps__timeline span {
    display: block;
    margin-block-end: 46px;
    color: var(--accent);
    font-size: 28px;
    font-weight: 900;
  }

  .med-steps__timeline p {
    margin: 12px 0 0;
  }

  .med-author {
    background: #fff8ea;
  }

  .med-photo--portrait {
    min-height: 540px;
  }

  .med-author__facts {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 12px;
    margin-block-start: 24px;
  }

  .med-author__facts span {
    padding: 14px;
    border: 1px solid var(--line);
    border-radius: 8px;
    background: rgba(255, 254, 250, 0.76);
    color: var(--ink);
    font-weight: 800;
    line-height: 1.35;
  }

  .med-final {
    background: linear-gradient(135deg, #f4faf6, #fff8ea);
  }

  .med-bonus {
    display: grid;
    gap: 8px;
    margin-block: 24px;
    padding: 18px;
  }

  .med-bonus strong {
    color: var(--green);
    font-size: 22px;
  }

  .med-bonus span {
    color: var(--ink);
    line-height: 1.45;
  }

  .med-photo--call {
    min-height: 260px;
  }

  .med-form {
    display: grid;
    gap: 14px;
    padding: 24px;
  }

  .med-form div > span {
    color: var(--green);
    font-size: 13px;
    font-weight: 900;
  }

  .med-form h3 {
    margin-block-start: 8px;
    font-size: 34px;
  }

  .med-form label {
    display: grid;
    gap: 8px;
    color: var(--ink);
    font-weight: 800;
  }

  .med-form input,
  .med-form textarea {
    width: 100%;
    border: 1px solid rgba(22, 33, 31, 0.16);
    border-radius: 8px;
    background: #f7f8f3;
    color: var(--ink);
    font: inherit;
    padding: 14px;
  }

  .med-form input:focus,
  .med-form textarea:focus {
    outline: 3px solid rgba(233, 75, 27, 0.18);
    border-color: var(--accent);
    background: #fff;
  }

  .med-form p {
    margin: 0;
    font-size: 15px;
  }

  .med-photo-guide {
    padding-block: 36px 72px;
    background: #fffefa;
  }

  .med-photo-guide__inner {
    display: grid;
    grid-template-columns: 0.7fr 1.3fr;
    gap: 32px;
    padding-block-start: 28px;
    border-block-start: 1px solid var(--line);
  }

  .med-photo-guide h2 {
    margin: 0;
    color: var(--ink);
    font-size: 30px;
    line-height: 1.1;
  }

  .med-photo-guide ul {
    display: grid;
    gap: 8px;
    margin: 0;
    color: var(--muted);
    line-height: 1.5;
  }

  .med-sticky {
    position: fixed;
    z-index: 20;
    right: 16px;
    bottom: 16px;
    display: none;
    min-height: 48px;
    padding-inline: 18px;
  }

  @media (max-width: 1040px) {
    .med-page h1 { font-size: 58px; }
    .med-page h2 { font-size: 44px; }

    .med-hero__inner,
    .med-leak__inner,
    .med-offer__inner,
    .med-author__inner,
    .med-final__inner,
    .med-photo-guide__inner {
      grid-template-columns: 1fr;
    }

    .med-hero__inner {
      min-height: auto;
    }

    .med-pain__grid,
    .med-method__diagram,
    .med-steps__timeline {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 640px) {
    .med-section {
      padding-block: 60px;
    }

    .med-hero {
      padding-block: 36px 42px;
    }

    .med-page h1 {
      font-size: 42px;
      line-height: 1.05;
    }

    .med-page h2 {
      font-size: 34px;
      line-height: 1.06;
    }

    .med-hero__lead,
    .med-leak__copy p,
    .med-method__intro p,
    .med-final__copy > p,
    .med-author__copy > p {
      font-size: 18px;
    }

    .med-hero__inner {
      gap: 28px;
    }

    .med-hero__actions,
    .med-btn,
    .med-form button {
      width: 100%;
    }

    .med-photo--hero {
      min-height: 180px;
    }

    .med-metrics {
      display: none;
    }

    .med-pain__grid,
    .med-method__diagram,
    .med-steps__timeline,
    .med-offer__items,
    .med-author__facts {
      grid-template-columns: 1fr;
    }

    .med-card span,
    .med-method__diagram span,
    .med-steps__timeline span {
      margin-block-end: 22px;
    }

    .med-note {
      font-size: 18px;
    }

    .med-compare__list article {
      grid-template-columns: 1fr;
    }

    .med-photo--portrait {
      min-height: 360px;
    }

    .med-form {
      padding: 18px;
    }

    .med-sticky {
      display: inline-flex;
    }
  }
</style>
